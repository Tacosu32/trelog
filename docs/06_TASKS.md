# 2026-06-10 完了 トレーナー選択 v1 画像/休憩仕上げ

- [x] ミナ、レイカ、アカリのプリセット画像パスを `assets/trainer/public/presets/{trainerId}/default.png`、`cheer.png`、`result.png`、`rest.png` に更新した。
- [x] プリセット画像のフォールバックを、選択中状態別画像、同トレーナーdefault、既存public状態別画像、既存public通常画像、CSS仮表示の順に確認した。
- [x] IndexedDBカスタム画像がある場合は、引き続きカスタム画像を優先する仕様を維持した。
- [x] 一時停止、再開、休憩チケット使用、休憩チケットで継続を守った日、休憩日、低負荷/休憩寄りコメントをトレーナー別コメントとして反映した。
- [x] リザルトEXPゲージの進捗色を赤、ピンク、オレンジ系の暖色へ再調整し、白く見えにくいようにした。
- [x] `APP_VERSION`、`APP_BUILD_LABEL`、`CACHE_NAME` を `0.8.2-dev` / `trainer-pause-images-2026-06-10` に更新した。
- [ ] カスタムトレーナー作成画面、口調自由入力UI、LLM API、音声、SE、通知、ウィジェット、グラフ、クラウド保存、アカウント機能は未実装のままにする。

# 06_TASKS

## 2026-06-10 完了: ホーム画面トレーナーヒーロー上部配置

- ホーム画面のトレーナー表示をタイトル直下へ移動した。
- 表示順を、タイトル / 日付、トレーナーヒーロー、ステータス、レベルゲージ、今日の目標、カレンダー、記録開始ボタン、バージョン表示に整理した。
- スマホ幅ではトレーナー画像とコメントを縦積みにし、広い幅では横並びに切り替えるようにした。
- カレンダーを今日の目標より下へ移動した。
- `APP_BUILD_LABEL` と `CACHE_NAME` を `home-trainer-hero-2026-06-10` に更新した。

## 2026-06-09 完了: ホーム画面のトレーナーモチベーション強化

- ホーム画面のダッシュボード直下に、大きめのトレーナー画像と吹き出しコメントを表示した。
- `HOME_TRAINER_LINES` と `getHomeTrainerLine(context)` を追加し、ホーム用コメントを状態別に分岐できるようにした。
- 今日の記録有無、目標達成、あと少し、連続日数、休憩チケット不足、直近の休憩チケット自動消費、時刻に応じたコメントを追加した。
- ホーム画像は default 状態のままにし、カスタム通常画像または `trainer_default.png` を使うよう維持した。
- `APP_VERSION`、`APP_BUILD_LABEL`、`CACHE_NAME` をホームトレーナー強化版へ更新した。

## 2026-06-09 完了

- 休憩チケットの日跨ぎ自動消費を、今日より前の日付だけを対象にして実装した。
- `trelog_state` に `lastContinuityCheckDate` と `restTicketEvents` を追加し、既存状態の補完に対応した。
- ホーム画面に記録日、休憩日、未記録日、今日、未来日を区別する月間カレンダーを追加した。
- カレンダーの前月 / 次月切り替えを追加した。
- DEBUG_MODE 用に連続記録操作、指定日操作、手動日跨ぎ判定、状態確認を追加した。

## 次に確認すること

- 実機スマホでカレンダーのセル内テキストが狭い画面でも崩れないか確認する。
- 複数日未記録かつ休憩チケット不足の状態で、未記録日が意図通り残るか確認する。

## 2026-06-02 追記

- GitHub Pages公開後に、Safari/Chrome/Discord内ブラウザでsafe areaと下部ボタンの実機確認を継続する。
- PWA更新時のバージョン表示とService Worker更新確認の案内文を、ユーザー向けにさらに分かりやすくする。
- 必要に応じて、更新確認後に新しいService Workerが有効になったことを通知するUIを検討する。

## 次にやること

- PWA用の正式アイコン `assets/icons/icon-192.png` と `assets/icons/icon-512.png` を制作する
- 正式アイコン制作時は、スマホのホーム画面で見やすい余白、角丸で切り抜かれても崩れない構図、トレログらしい緑系テーマとの相性を確認する
- 正式アイコン作成後、`manifest.json` の icons にPNGアイコン参照を追加し直す
- GitHub Pages公開後、公開URLでmanifestとservice workerが正しく読めることを確認する
- PWAとしてのインストール確認を、Chrome DevToolsのApplicationパネルとスマホ実機で行う
- 音楽プレイリスト機能を検討する
- 複数音源アップロードを検討する
- 音楽のループ再生を検討する
- セッション中の音楽停止を一時停止扱いにするか、先頭戻しにするかを正式仕様として検討する
- リザルト画面の演出、文言、報酬表示をスマホ実機で確認する
- 履歴のグラフ化を検討する
- 週単位、月単位、年単位の推移表示を検討する
- 種目別スコア推移を検討する
- 履歴専用タブ化を検討する
- ユーザー情報に基づくスコア係数調整を検討する
- カウンセリング形式の初期設定を検討する
- 自作種目登録機能を検討する
- 自作種目の記録方式・係数設定を検討する
- LLMによる種目負荷判定と係数提案を検討する
- 体力レベル別の評価プロファイルを検討する
- カウンセリング形式で評価プロファイルを自動設定する
- LLMによるユーザー状態のヒアリングを検討する
- 目的別スコア係数調整を検討する
- 種目ごとの個別係数調整を検討する
- 自作種目登録時の負荷判定を検討する
- スコア調整パネルで検証した設定を `DEFAULT_SCORING_CONFIG` に正式反映する運用を整える
- 公開用トレーナー画像 `trainer_default.png`、`trainer_cheer.png`、`trainer_result.png`、`trainer_rest.png` を正式制作する
- アプリ内アップロード画像をIndexedDBへ保存し、GitHubに個人素材を含めない運用を検討する
- トレーナー選択画面を検討する
- トレーナー画像差し替えを検討する
- 口調設定を検討する
- カスタムトレーナー作成を検討する
- LLMによる動的セリフ生成を検討する
- セッション中SEを検討する
- 音楽プレイリストを検討する
- カスタムトレーナー画像本体も含めた完全バックアップを検討する
- 複数トレーナー画像差分のアップロードを検討する
- IndexedDBデータの管理UI改善を検討する
- クラウド同期を検討する
- アカウント連携を検討する
- 4差分カスタム画像の容量表示と個別バックアップ対象選択を検討する
- カスタム画像のトリミング、位置調整、プレビュー改善を検討する
- バックアップJSONのサイズ表示とエクスポート前確認を検討する
- 完全バックアップから画像だけ復元するUIを検討する
# 2026-06-10 追記: カウンセリング機能 v1

- [x] 設定画面にカウンセリングセクションを追加する。
- [x] 運動経験、目的、希望頻度、運動時間、評価の厳しさ、得意/苦手メニューを入力できるようにする。
- [x] ルールベースでスコア設定案を生成する。
- [x] おすすめ評価プロファイル、今日の目標スコア、種目ごとの係数、目安、トレーナーコメントを表示する。
- [x] 提案結果を数値編集できるようにする。
- [x] 「この設定を適用」で `trelog_dev_scoring_config` と評価プロファイルへ反映する。
- [x] カウンセリング結果の破棄とデフォルト設定へのリセットを追加する。
- [x] カウンセリング結果をバックアップ/復元に含める。
- [x] APP_VERSION / APP_BUILD_LABEL / CACHE_NAME を更新する。
- [ ] LLM API接続、通知、グラフ表示、クラウド保存は未実装のままにする。
# 2026-06-10 追記: リザルトEXP演出

- [x] リザルト画面に記録前EXP、獲得EXP、記録後EXPを表示する。
- [x] EXPゲージを記録前状態から記録後状態へアニメーションさせる。
- [x] レベルアップ時に `Lv.X → Lv.Y` と段階的なゲージ演出を表示する。
- [x] 複数レベルアップ時の最低限の段階表示に対応する。
- [x] 5レベルごとの休憩チケット報酬をリザルトに表示する。
- [x] 今日の目標達成、レベルアップ、報酬獲得時に紙吹雪を表示する。
- [x] 演出中でもOKボタンで閉じられるようにする。
- [x] APP_VERSION / APP_BUILD_LABEL / CACHE_NAME を更新する。
- [ ] SE、音声、複雑なアニメーションライブラリは未実装のままにする。
# 2026-06-10 追記: トレーナー選択 v1

- [x] リザルトEXPゲージの背景色と進捗色を修正する。
- [x] プリセットトレーナーデータ `TRAINER_PRESETS` を追加する。
- [x] ミナ、レイカ、アカリの3人をプリセットとして用意する。
- [x] 設定画面にトレーナー選択UIを追加する。
- [x] 選択中トレーナーIDを `trelog_state.selectedTrainerId` に保存する。
- [x] ホーム、セッション、リザルト、休憩コメントに選択中トレーナーの画像とセリフを反映する。
- [x] 画像フォールバック順を、カスタム画像、選択中プリセット画像、public画像、仮表示の順にする。
- [x] APP_VERSION / APP_BUILD_LABEL / CACHE_NAME を更新する。
- [ ] カスタムトレーナー名を設定できるUIを検討する。
- [ ] 口調自由入力UIを検討する。
- [ ] カスタムトレーナー画像4差分とセリフセットを、トレーナー選択画面へ追加できる構造を検討する。
- [ ] カスタムトレーナー作成画面、LLM API接続、通知、クラウド保存は未実装のままにする。
# 2026-06-10 完了 トレーナー選択 v1 仕上げ

- [x] リザルトEXPゲージの背景を薄いベージュ/グレー系、進捗を赤〜ピンク〜オレンジ系に調整した。
- [x] ミナ、レイカ、アカリのホーム・セッション・リザルト・休憩/継続系コメントを拡充した。
- [x] コメントを `TRAINER_PRESETS.lines.home/session/result/rest` に集約し、選択中トレーナーのセリフを優先する構造を維持した。
- [x] カスタム画像優先時の説明をトレーナー選択画面へ追加した。
- [x] プリセット専用画像が未設定でも既存public画像へフォールバックする構造を維持した。
- [x] `selectedTrainerId` は既存の `trelog_state` バックアップ/復元対象として保持されることを確認した。
- [x] `APP_VERSION`、`APP_BUILD_LABEL`、`CACHE_NAME` を当時のトレーナー選択v1仕上げ用ビルド値に更新した。
- [ ] カスタムトレーナー作成画面、口調自由入力UI、LLM API接続、音声、SE、通知、ウィジェット、グラフ、クラウド保存、アカウント機能は未実装のままにする。

# 2026-06-10 完了 設定画面アコーディオン化

- [x] 設定画面を基本設定、トレーナー設定、カウンセリング、スコア調整、データ管理、アプリ更新、デバッグにカテゴリ分けした。
- [x] 各カテゴリを `details` / `summary` によるアコーディオンUIへ整理した。
- [x] 基本設定を初期状態で開き、デバッグを含むその他カテゴリを初期状態で閉じるようにした。
- [x] 設定カテゴリの開閉状態を `trelog_settings_sections_state` に保存し、リロード後も維持するようにした。
- [x] カテゴリ見出しの高さ、開く/閉じる表示、説明文サイズ、スマホ1カラム化を調整した。
- [x] `APP_VERSION`、`APP_BUILD_LABEL`、`CACHE_NAME` を `0.9.0-dev` / `settings-accordion-2026-06-10` に更新した。
- [ ] カスタムトレーナー作成画面、口調自由入力UI、LLM API接続、通知、ウィジェット、グラフ表示、クラウド保存、アカウント機能は未実装のままにする。
